from setuptools import setup, find_packages

setup(
    name="AdGuardX",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "dnspython",
        "flask",
    ],
    entry_points={
        "console_scripts": [
            "adguardx=adguardx.main:main",
        ],
    },
)
